---
description: Performance profiling and reliability safeguards for Android Compose apps.
globs: ["**/*.kt"]
alwaysApply: true
---

# Performance

- Minimize recomposition via state scoping and memoization.
- Use lazy containers for lists; paginate network calls.

# Reliability

- Retry transient network failures with backoff where safe.
- Persist minimal state needed for recovery; avoid data corruption.
